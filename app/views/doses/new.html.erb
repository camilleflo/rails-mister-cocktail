
<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
		<h1><%= @cocktail.name %></h1>
      		<h2> Liste des ingrédients </h2>
      	<%= simple_form_for ([@cocktail, @dose]) do |f| %>
    	<%= f.association :ingredient %>
      <%= f.input :description %>

        <%= f.submit "Add to your cocktail", class: "btn btn-primary" %>
        <button type="button" class="btn btn-success"><%= link_to "Go Back", cocktail_path(@cocktail) %></button>

      <% end %>
    </div>
  </div>
</div>

