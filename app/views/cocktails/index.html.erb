<h1> MES SUPERS COCKTAILS </h1>

<% @cocktails.each do |cocktail| %>
  	<h2><%= link_to cocktail.name, cocktail_path(cocktail)%></h2>
  <% end %>
	<button type="button" class="btn btn-success"><%= link_to"Create a cocktail", new_cocktail_path %></button>
